import React from 'react';
import useStyles from './channelsStyles';

export const Channels = () => {
    const classes = useStyles();
    return(
        <div><h1>Channels Router is working!</h1></div>
    );
}